# PEM demo using VirtualBox

## Demo prep
Run `vagrant up`

SSH into the console node using `vagrant ssh console`

Become root using `sudo - su`

Run `00-provision.sh` from the `/vagrant` directory.

